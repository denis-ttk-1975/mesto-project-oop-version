//утилитарные функции

/*
//функция закрытия попапа при нажатии ESC
const closeESC = (event) => {
    const popupOpened = document.querySelector(".popup_opened");
    if (event.key === 'Escape') {
      closePopup(popupOpened)
    }
  }
  window.addEventListener('keydown', closeESC) //обработчик нажатия ESC*/